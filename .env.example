# Copy to .env and fill in your keys
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...        # Optional — only if provider=openai in config.yaml
NVD_API_KEY=                 # Optional — raises NVD rate limit from 5 to 50 req/s
